server:
  port: 8081

pushbullet.ribbon.listOfServers: https://api.pushbullet.com

spring:
  main:
    banner-mode: "off"
  jpa:
    database: h2
    hibernate:
      dialect: org.hibernate.dialect.H2Dialect
      ddl-auto: create-drop

# Do not edit below this line
feign:
  hystrix:
    enabled: false

ribbon:
  eureka:
    enabled: false
